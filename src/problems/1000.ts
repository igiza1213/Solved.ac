export const solution = (stdinInput: string) => {
    const [N, M, circle] = stdinInput
      .trim()
      .split(" ")
      .map((value) => parseInt(value));
  
      console.log(N, M, circle)
  };
  
  // solution(input);
  